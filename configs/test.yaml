---
defaults:
  - dataset: toy-test

setup:
  seed: 42
  saved-models-path: ../saved/logs/runs/exp14-features/toy_model.pt
  eval-output-path: evaluation_output
  tensorboard-dir: ../saved/logs/runs

model:
  hidden-channels: 16
  num-blocks: 1
  parameters: null

hydra:
  run:
    dir: outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}
